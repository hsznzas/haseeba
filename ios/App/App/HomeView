import SwiftUI

struct HomeView: View {
    var body: some View {
        ZStack {
            Color(hex: "#0f172a").ignoresSafeArea() // Background Color
            
            VStack(spacing: 20) {
                Image(systemName: "flame.fill")
                    .font(.system(size: 60))
                    .foregroundStyle(.orange)
                
                Text("Native Swift Home")
                    .font(.largeTitle)
                    .fontWeight(.bold)
                    .foregroundColor(.white)
                
                Text("If you see this, the port worked!")
                    .font(.body)
                    .foregroundColor(.gray)
            }
        }
    }
}

#Preview {
    HomeView()
}
